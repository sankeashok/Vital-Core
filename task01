# Task List - Project Vital-Core Implementation

- `[x]` Phase 1: Core Configuration and API Gateway Lifespan Hooks
    - `[x]` Create production dependencies list (`requirements.txt`)
    - `[x]` Create environment settings manager (`src/core/config.py`)
    - `[x]` Create Pydantic schema validation models (`src/api/schemas.py`)
    - `[x]` Create API gateway and lifespan startup hook (`src/api/gateway.py`)
- `[x]` Phase 2: Lightweight Custom Feature Store SDK
    - `[x]` Create feature store connector (`src/core/feature_store.py`)
    - `[x]` Seed offline data and online stores inside SQLite DB
- `[x]` Phase 3: Drift Detection and Model Retraining Pipeline
    - `[x]` Create KS-Test and PSI drift calculator (`src/ml/drift.py`)
    - `[x]` Write training pipeline with Optuna hyperparameter optimization (`src/ml/train.py`)
    - `[x]` Implement local SQLite-backed MLflow Registry Promotion gate (`src/ml/registry.py`)
- `[x]` Phase 4: Hugging Face Clinical Copilot and Main UI Entrypoint
    - `[x]` Create serverless LLMOps Client (`src/copilot/clinical_copilot.py`)
    - `[x]` Implement static glassmorphic telemetry dashboard index page
    - `[x]` Connect API app lifespan and routers (`src/main.py`)
- `[/]` Phase 5: Verification & DevOps Hardening
    - `[ ]` Write Pytest test files (`tests/test_drift.py`, `tests/test_gateway.py`)
    - `[ ]` Create `Dockerfile` and GHA workflow file
    - `[ ]` Complete validation and summary walkthrough
